generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = "mysql://root:Nikolicmarko9827**@localhost:3306/icr"
}

model user {
    id            Int     @id @unique
    date_time     String?
    username      String
    email         String
    password      String?
    adress        String?
    phone         BigInt  @unique
    plane_best    Int
    login_history Int

    @@map(name: "user")
}

model flights {
    flights_id Int   @id
    name       String
    loc_a      String
    loc_b      String
    airport_a  String // ->>>
    airport_b  String // <<<-


    @@map(name: "flights")
}
